# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Published data snapshots — generated by `council publish`. NOT committed
# yet: contains named-individual claims (e.g. recusal rates) with
# documented MODERATE-HIGH defamation exposure per docs/strategy/
# PRIVATE_ASSESSMENT.md. Do not un-ignore this until either (a) that doc's
# mitigation list is done (About page, corrections contact, named-driver
# claims moved out of headline-level rendering, media lawyer consult), or
# (b) the defamation-auditor pass has cleared a given publish run. See
# docs/TESTING.md.
public/data/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
